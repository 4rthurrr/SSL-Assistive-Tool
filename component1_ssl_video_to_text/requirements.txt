# SSL Video-to-Text Translation Requirements

# Core ML frameworks
torch>=2.0.0
torchvision>=0.15.0
tensorflow-cpu>=2.13.0  # Alternative to PyTorch if needed

# Computer vision and video processing
opencv-python>=4.8.0
mediapipe>=0.10.0
albumentations>=1.3.0
Pillow>=10.0.0
imageio>=2.31.0
scikit-image>=0.21.0

# Data handling and utilities
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.11.0
h5py>=3.9.0
tqdm>=4.65.0

# Machine learning utilities
scikit-learn>=1.3.0
matplotlib>=3.7.0
seaborn>=0.12.0
plotly>=5.15.0

# API and web framework
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
python-multipart>=0.0.6
aiofiles>=23.0.0
pydantic>=2.0.0

# Audio and text processing (for WER calculation)
jiwer>=3.0.0
editdistance>=0.6.2
librosa>=0.10.0  # If audio processing is needed
nltk>=3.8.0

# Configuration and serialization
pyyaml>=6.0
toml>=0.10.2
jsonschema>=4.17.0

# Logging and monitoring
tensorboard>=2.13.0
wandb>=0.15.0  # Optional: for experiment tracking
loguru>=0.7.0

# Performance optimization
numba>=0.57.0  # JIT compilation
psutil>=5.9.0  # System monitoring

# Development and testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
black>=23.7.0  # Code formatting
flake8>=6.0.0  # Linting
mypy>=1.5.0  # Type checking

# Production deployment
gunicorn>=21.0.0
docker>=6.1.0  # For containerization
redis>=4.6.0  # Optional: for caching

# Additional utilities
requests>=2.31.0
aiohttp>=3.8.0
python-jose>=3.3.0  # JWT handling if authentication needed
passlib>=1.7.4  # Password hashing if needed
python-dotenv>=1.0.0  # Environment variable management

# GPU support (uncomment if using NVIDIA GPUs)
# torch-audio>=2.0.0  # GPU accelerated audio processing
# cupy-cuda11x>=12.0.0  # GPU accelerated NumPy operations

# Optional: Model optimization
# onnx>=1.14.0  # ONNX model format
# onnxruntime>=1.15.0  # ONNX inference
# tensorrt>=8.6.0  # TensorRT optimization (NVIDIA only)

# Optional: Distributed training
# torch-distributed>=0.1.0
# horovod>=0.28.0

# Version constraints for compatibility
setuptools>=68.0.0
wheel>=0.41.0
pip>=23.2.0
